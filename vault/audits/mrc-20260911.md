# Migration Readiness Certificate — snapshot 20260911

Canonical one-pager: `docs/migration-readiness-certificate.md` (committed this session).

This dated snapshot marks delivery of §A.4 / M3.5. The MRC states:
(i) Z13-specific components → per-host replacement (incl. init-host, GPU driver,
    gfx1151 Vulkan/OEM kernel, BIOS VRAM/GTT as Owner firmware step);
(ii) the exact clean-OS rebuild sequence (init-host → bootstrap → submodule →
    runtime → fetch-models → secrets → host bits → systemd → verify);
(iii) measured rebuild time (container-verified prerequisites; full GPU rebuild
    measured on W3 real machine, < 2h per §K M3.2);
(iv) known blockers (BIOS VRAM/GTT, hermes/bin symlinks, secrets, sshd pin,
    101GB weights, §C.5 candidate URLs, deferred SanDisk/Google).
